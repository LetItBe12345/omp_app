# 13 外部参考仓库初始化

## 目标

提供可重复执行的脚本，将四个外部参考仓库浅克隆到 `omp_app` 并列的
`omp-references/` 目录，供代码检索使用。

## 完成条件

- [x] 脚本默认使用 `../omp-references/`，也允许显式指定目录。
- [x] 四个仓库使用浅克隆，完整工作树可直接检索。
- [x] 重复执行不会覆盖已有仓库。
- [x] 已验证四个仓库的目录、远端地址、浅克隆状态和当前 commit。
- [x] 使用方法写入参考项目文档。
